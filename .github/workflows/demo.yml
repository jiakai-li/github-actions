name: Deploy website
on:
  workflow_dispatch:
  push:
    branches:
      - main
jobs:
  dev:
    environment: Dev
    runs-on: ubuntu-latest
    env:
      MY_ENV: ${{ secrets.MY_ENV }}
      ENV: ${{ vars.ENV }}
    steps:
      - name: Prepare
        run: echo "Prepare running reusable component"
      - name: Call reusable component
        uses: ./.github/workflow/sub_workflow
        with:
          my_parameter: Parameter Passed In
