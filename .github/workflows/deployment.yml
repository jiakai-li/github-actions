name: Test Project
on:
  issues:
  workflow_dispatch:
  pull_request:
    types:
      - opened
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: check event details
        run: echo "${{ toJSON(github.event) }}"
